# Example Environment Configuration for Schedulink
# Copy this file to .env.production and update with your actual values

# Production Environment Variables
DATABASE_URL=postgresql://schedulink_user:YOUR_DB_PASSWORD@localhost:5432/schedulink_db
SECRET_KEY=your-super-secret-key-here-change-this-in-production
ENVIRONMENT=production
DEBUG=false

# EC2 and Domain Configuration
EC2_PUBLIC_IP=YOUR_EC2_PUBLIC_IP
DOMAIN_NAME=YOUR_DOMAIN_NAME

# Frontend URL
FRONTEND_URL=http://YOUR_EC2_PUBLIC_IP:3000

# Backend settings
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Database settings (for PostgreSQL in production)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=schedulink_db
DB_USER=schedulink_user
DB_PASSWORD=YOUR_SECURE_DATABASE_PASSWORD

# CORS settings
ALLOWED_ORIGINS=http://YOUR_EC2_PUBLIC_IP:3000,https://YOUR_DOMAIN_NAME

# Notes:
# - Replace ALL CAPS values with your actual configuration
# - Use strong passwords for production
# - Keep this file secure and don't commit to version control
# - Run ./setup-env.sh for automated configuration
