FROM python:3.10

WORKDIR /app

COPY ./requirements.txt /app/requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY . /app

# Use the simple main file for testing
CMD ["uvicorn", "simple_main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
